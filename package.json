{
  "name": "k6-load-gen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json",
    "build:cli": "esbuild src/timeline/cli.ts --bundle --platform=node --format=cjs --outfile=dist/timeline-cli.js && esbuild src/storage/index-cli.ts --bundle --platform=node --format=cjs --outfile=dist/index-cli.js && esbuild src/fleet/cli.ts --bundle --platform=node --format=cjs --outfile=dist/fleet-cli.js && echo '{\"type\":\"commonjs\"}' > dist/package.json",
    "aggregator-configs": "tsx src/aggregator/cli.ts"
  },
  "devDependencies": {
    "@types/k6": "^1.0.2",
    "@types/node": "^22.10.2",
    "esbuild": "^0.28.2",
    "tsx": "^4.23.13",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
